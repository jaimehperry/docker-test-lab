machine:
  node:
    version: 0.10.22
  services:
    - docker

dependencies:
    pre:
        - sudo pip install docker-compose
    post:
        - docker-compose up: background:true
test:
   post: 
        - docker-compose stop
