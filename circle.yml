machine:
  node:
    version: 0.10.22
  services:
    - docker

dependencies:
    pre:
        - sudo pip install docker-compose
    post:
        - docker-compose up -d
test:
   post: 
        - docker-compose stop
